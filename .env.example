# Backend (FastAPI) — never commit real keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol
FRONTEND_ORIGIN=http://localhost:3000
# Optional overrides (defaults point at the repo):
# PROBLEMS_ROOT=
# KNOWLEDGE_ROOT=
